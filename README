#
#
#   kodiakDNS - DNS Gathering Tool
#
#       _         _  
#      ((`'-"""-'`)) 
#       )  -   -  (
#      /   x _ x   \     コディアック`DNS
#      \   ( + )   /
#       '-.._^_..-'
# 


  - Retrieving NS,MX,A,PTR records
  - Retrieving AXFR
  - Retrieving subdomains by bruteforcing
  - Retrieving DNS version
  - Retrieving AS Numbers
  - Retrieving IPs in DNS-based block list information/database


Dependences:

  fpdns (Net::DNS::Fingerprint)

  Download tar.gz from https://github.com/kirei/fpdns/downloads

  $ cd fpdns/
  $ perl Makefile.PL
  $ make
  $ make install
  $ sudo make install


Usage:

   $ perl kodiakDNS.pl <domain>
